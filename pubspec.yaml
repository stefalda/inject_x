name: inject_x
description: "A minimalistic Dependency Injestion solution"
version: 0.0.3
homepage: https://github.com/stefalda/inject_x
issue_tracker: https://github.com/stefalda/inject_x/issues

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
#dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
